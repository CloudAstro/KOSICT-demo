replicaCount: 1

image:
  tag: "7.4.0-alpine"

redis:
  maxmemory: "256mb"
  maxmemoryPolicy: "allkeys-lru"

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
