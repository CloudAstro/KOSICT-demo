replicaCount: 1

image:
  tag: "7.4.0"

redis:
  password: "changeme-use-sealed-secrets"
  maxmemory: "2gb"
  maxmemoryPolicy: "allkeys-lru"

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi
