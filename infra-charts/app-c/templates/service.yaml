apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-c.fullname" . }}
  labels:
    {{- include "app-c.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgresql
      protocol: TCP
      name: postgresql
  selector:
    {{- include "app-c.selectorLabels" . | nindent 4 }}
